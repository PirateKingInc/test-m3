// Heat system: passive radiator decay, coolant actions, meltdown/core
// destruction checks. Stub for now — implemented in the Spawner+Heat
// systems PR.

/**
 * @param {object} state GameState
 * @param {number} dt seconds
 */
export function step(state, dt) {
  return state;
}
