// Combat system: enemy movement, turret targeting/fire, core damage,
// build/upgrade action handling. Stub for now — implemented in the
// Combat+Economy systems PR.

/**
 * @param {object} state GameState
 * @param {number} dt seconds
 */
export function step(state, dt) {
  return state;
}
