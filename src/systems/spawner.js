// Spawner system: enemy generation and wave/threat progression.
// Stub for now — implemented in the Spawner+Heat systems PR. Kept as a
// no-op here so core/simulate.js has a stable four-system pipeline from
// the start.

/**
 * @param {object} state GameState
 * @param {number} dt seconds
 */
export function step(state, dt) {
  return state;
}
