{
  "name": "reactor-siege",
  "version": "1.0.0",
  "description": "A systems-driven reactor defense game. Pure deterministic game logic on canvas, no runtime dependencies.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "simulate": "node tools/simulate.js"
  },
  "engines": {
    "node": ">=20"
  }
}
